sudo: true
language: python
python:
  - "2.7"
# command to install dependencies

before_install:
  - sudo sh install_debian_script.sh

install: pip install -r requirements.txt
# command to run tests
script: sh run_travis.sh
